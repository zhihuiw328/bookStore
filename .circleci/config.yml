version: 2.1



jobs:
  build:
    working_directory: ~/circleci-java
    docker:
      - image: "circleci/openjdk:8-jdk"
    steps:
      - checkout
      - run: ./gradlew build

workflows:
  build:
    jobs:
      - build
