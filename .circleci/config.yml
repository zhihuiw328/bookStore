version: 2.1

orbs:
  heroku: circleci/heroku@2.0.0

jobs:
  build:
    working_directory: ~/circleci-java
    docker:
      - image: "circleci/openjdk:8-jdk"
    steps:
      - checkout
      - run: chmod +x ./gradlew
      - run: ./gradlew build
  deploy:
    executor: heroku/default
    steps:
      - checkout
      - heroku/deploy-via-git:
          app-name: "book-demo-store"
          branch: "master"

workflows:
  build_and_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build