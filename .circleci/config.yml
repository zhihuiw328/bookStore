version: 2.1

orbs:
  openjdk: circleci/openjdk@1.3.0
  gradle: circleci/gradle@1.1.0

jobs:
  builds:
    working_directory: ~/circleci-python
    docker:
      - image: "circleci/openjdk:8-jdk"
    steps:
      - checkout
      - run: ./gradlew build

workflows:
  build:
    job:
      - build
